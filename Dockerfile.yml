FROM postgres:latest

# Copie du script shell
COPY testbdd.sh /testbdd.sh

# Copie du script SQL d'initialisation
COPY testbdd.sh /testbdd.sh


# Définir les variables d'environnement pour la base de données
ENV POSTGRES_USER=${{ secrets.PHX_DBUSER }}
ENV POSTGRES_PASSWORD=${{ secrets.PHX_DBPASSWORD }}


# Point d'entrée
ENTRYPOINT [ "/testbdd.sh" ]